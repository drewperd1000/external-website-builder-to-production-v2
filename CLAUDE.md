# external-website-builder-to-production-v2 - AI Context

> Codesight (cloud note): in a session without the codesight MCP server, the `mcp__codesight__*` tools won't exist. That is NOT 'codesight unavailable' -- read `.codesight/CODESIGHT.md` (plus routes/schema/components and `.codesight/wiki/index.md` if present) directly for orientation.
